# Pipeline artifacts — parquet, embeddings, checkpoints, MLflow store (trd.md §4, §11).
# Multi-GB. Regenerated by `contentsignal ingest` and downstream stages.
artifacts/
mlruns/

# Raw Kaggle download. Deleted after parquet conversion (prd.md §2), but never tracked.
data/
*.csv
*.csv.zip

# Credentials
kaggle.json
.env
.env.*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Notebooks
.ipynb_checkpoints/

# macOS
.DS_Store

# NOTE: reports/ is deliberately NOT ignored. reports/metrics/*.json is the
# committed record of every run's numbers, and `make report` regenerates
# reports/results.md from it (trd.md §11).
